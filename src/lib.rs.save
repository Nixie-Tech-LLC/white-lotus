mod config;
mod message;
mod action;
mod membership;
mod broadcast; 
mod gossip;

//peers name/NodeId marker trait - this is the peers public key placeholder
pub trait NodeId: Copy + Eq + Ord + std::hash::Hash {}
impl<T: Copy + Eq + Ord + std::hash::Hash> NodeId for T {}
//contents of the gossip - opqque payload ie a file hash announcment -
pub trait Payload: Clone {} 
impl<T: Clone> Payload for T {}



